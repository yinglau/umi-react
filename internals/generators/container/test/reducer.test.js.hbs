/**
 * {{ properCase name }} reducer test
 * author: {{ author.name }}
 * email: {{ author.email }}
 */

import {{ camelCase name }}Reducer, { initialState } from '../reducer'
import { fromJS } from 'immutable'
import { DEFAULT } from '../constants'

describe('test {{ camelCase name }} reducer', () => {
  it('should return initialState', () => {
    expect({{ camelCase name }}Reducer(undefined, {})).toEqual(initialState)
  })

  it('state.title should equal `myname`', () => {
    const state = fromJS({
      defaultState: 'myname'
    })
    expect({{ camelCase name }}Reducer(undefined, {type: DEFAULT, defaultState: 'myname'})).toEqual(state)
  })
})
