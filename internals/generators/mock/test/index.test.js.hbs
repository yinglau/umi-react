/**
 * {{ properCase name }} component test
 * author: {{ author.name }}
 * email: {{ author.email }}
 */

import "@babel/polyfill"
import React from 'react'
import { shallow } from 'enzyme'

import { {{properCase name}} as Component} from '../'

describe('test {{properCase name}}', () => {
  {{#if isFunctional }}
  it('{{properCase name}} has one div element', () => {
    const wrapper = shallow(<Component />)
    expect(wrapper.find('div')).toHaveLength(1)
  })
  {{else}}
  it('instance of {{properCase name}}', () => {
    const props = {
      dispatch: () => {}
    }
    const wrapper = shallow(<Component {...props} />)
    const instance = wrapper.instance()
    expect(instance).toBeInstanceOf(Component)
  })
  {{/if}}
})
