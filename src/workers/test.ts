import request from '../utils/fetch';
export default () => {
  // self.onmessage = (e: any) => {
  // setInterval(() => {
  //   self.postMessage('hahaha');
  // }, 1000);
  // };
  // request('https://cnodejs.org/api/v1/topics').then(res => console.log(res));
};
